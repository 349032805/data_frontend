'use strict';

describe('Directive: chSideNavigation', function () {

  // load the directive's module
  beforeEach(module('chopperApp'));

  var element,
    scope;

  beforeEach(inject(function ($rootScope) {
    scope = $rootScope.$new();
  }));

});
