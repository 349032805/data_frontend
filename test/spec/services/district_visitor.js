'use strict';

describe('Service: districtVisitor', function () {

  // load the service's module
  beforeEach(module('chopperApp'));

  // instantiate service
  var districtVisitor;
  beforeEach(inject(function (_districtVisitor_) {
    districtVisitor = _districtVisitor_;
  }));
});
