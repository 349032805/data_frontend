'use strict';

describe('Service: zhaimiHttpInterceptor', function () {

  // load the service's module
  beforeEach(module('chopperApp'));

  // instantiate service
  var zhaimiHttpInterceptor;
  beforeEach(inject(function (_zhaimiHttpInterceptor_) {
    zhaimiHttpInterceptor = _zhaimiHttpInterceptor_;
  }));


});
